# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.idea
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NEVER commit secrets or vault paths)
.env
.env.local
.env*.local

# netlify
.netlify

# ── PRIVACY: raw statements / private working data must never be committed ──
# Only dashboard-safe JSON in /data is intended for the repo.
/uploads
/private
*.statement.pdf
*.statement.csv
# Private chat / decision logs may quote raw statement text — keep local only.
/data/chat-history.json
/data/reconciliation-decisions.json

# pdf-parse test fixture (required on disk but not committed)
/test/data/05-versions-space.pdf
